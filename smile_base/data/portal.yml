-
  I uninstall portal
-
  !python {model: ir.module.module}: |
    ids = self.search(cr, uid, [('name', 'ilike', 'portal')], limit=1, context=context)
    if ids:
      self.write(cr, uid, ids, {'auto_install': False}, context)
      for module in self.browse(cr, uid, ids, context):
        if module.state in ('installed', 'to upgrade'):
          module.button_uninstall()
